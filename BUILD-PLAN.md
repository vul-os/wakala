# Wakala autonomous build plan

The wave backlog for the automated build loop. Each loop iteration: read this + `DECISIONS.md`,
pick the highest-priority unblocked wave, dispatch Sonnet sub-agents to do it, verify
(build+test green), commit+push, then tick the wave and append any decisions to `DECISIONS.md`.

## Autonomous window
- **Start:** 2026-07-23T07:32:13+0200 (epoch 1784784734)
- **Stop at:** 2026-07-23T17:32:13+0200 (epoch 1784820734) — ~10 hours, 15-min cadence (~40 iterations)
- **Stop rule:** when `date +%s` ≥ 1784820734 **or** all waves are `DONE`, delete the cron and stop.

## Hard rules (never violate)
- All-Rust. Depend on `kotva-core`/`kotva-mail` by **pinned tag** (`core-v0.2.0`+), never HEAD/path.
- Every broker kind content-blind per spec (relay=blind, media-relay/reachability-adapter=blind-routing,
  gateway=terminating). Declared visibility must match reality (COORD-4/5).
- No token; stake/settle in existing assets only (DIRECTION §5).
- Preserve the Go relay + JS client until the Rust port is proven. Don't modify the kotva **spec**
  prose (the crates/ Rust is fair game). Log spec gaps to `COORDINATION.md`.
- Keep each repo **green** (build+test) at every commit. Never commit a broken tree.

## Waves

| # | Wave | Status | Notes |
|---|---|---|---|
| W1 | **envoir → node-only, green + committed** | DONE (620a68c) | gateway + its conformance/fuzz removed; substrate consumed from kotva-core@core-v0.2.0 (cargo package-rename alias, zero source churn); conformance-runner carries its own vectors.json snapshot; envoir build+test green, pushed. Unblocks W2 + envoir README |
| W2 | **Relocate gateway conformance + fuzz** envoir→wakala | DONE | 7 DMTAP §7 conformance cases ported to `crates/gateway/tests/conformance_gateway.rs` as real `#[test]`s against the live `gateway` API: GWALIAS-01/02, GWNAME-02, GWATT-05, LEG-01/02/03; `gateway::` in place of `envoir_gateway::`, `kotva_core::` in place of `dmtap_core::`, most call shapes unchanged. **Dropped:** GWATT-04 (already a documented skip in envoir itself — no KT-anchored second assurance tier exists in `gateway::attestation`/`provenance` either; noted in-file, not silently omitted). New self-contained `crates/gateway/fuzz/` cargo-fuzz project (own `[workspace]`, not a main-workspace member): `gateway_admission` ported 1:1 onto `gateway::authz::IdentityRegistry::admit`; `gateway_alias` **retargeted** onto `gateway::forwarded_addr::{encode,decode}` (envoir's original fuzzed `envoir-node`'s key-derived alias round-trip, which has no analogue in this crate — wakala's own key-derived alias is a one-way hash by design). Both targets built clean under `cargo +nightly fuzz build` and ran crash-free for ~300k/389k execs each in a local smoke run. `cargo test -p gateway` 313 tests green (306 pre-existing + our 7), `cargo build --workspace` green, `cargo clippy -p gateway --all-targets` clean (gateway stays exempt from workspace forbid-unsafe, untouched). |
| W3 | **broker-economics adopts real kotva-core** | DONE | dropped the stub `kotva_core` seam; real `IdentityKey`/`verify_domain`/deterministic-CBOR from the tag-pinned `kotva-core`; `Descriptor::sign`/`SignedDescriptor::verify`, `Tariff::sign`/`verify`, `UsageReceipt::sign`/`verify` (Ed25519, DS-tagged, canonical §18.1.1 CBOR, CONTRACT §6). Wire layout chosen + logged to `COORDINATION.md` for spec ratification. broker-conformance/gateway/reachability-adapter updated to real keypairs; 349 workspace tests green, clippy clean. |
| W4 | **reachability-adapter SNI/tunnel transport** | DONE (9bd0fc0) | SNI-passthrough peek + yamux reverse tunnel + fail-closed RST ingress; 16 tests. **REACH-2 gap:** box↔adapter control channel is unauthenticated plain TCP (no key-auth to box IK) — kotva-core identity is now pinned in the workspace (W3), but the auth wiring itself is still not built. NOT public-safe until then. |
| W4b | **REACH-2 tunnel key-auth** (closes W4 gap) | DONE (8d8eddc) | challenge-response: box signs `nonce‖name` under DS tag `WAKALA-REACH-v0/tunnel-auth`, adapter verifies against claimed IK (kotva-core Ed25519); single-use CSPRNG nonce (replay-inert); `TunnelRegistry` binds name→owning IK (no cross-IK hijack); 24 tests. **Residual (honest, in-code):** control channel is still plain TCP — key-auth closed (identity unforgeable), but transport confidentiality/integrity (libp2p Noise) not yet wired; on-path attacker can observe/DoS but not impersonate |
| W5 | **relay crate** (mesh, blind/structural) | DONE | new crate `crates/relay`: `RelayServer` (real libp2p 0.56 Circuit Relay v2 server — `relay`+`identify` over TCP/Noise/Yamux, matching envoir dmtap-p2p's libp2p line) + `RelayCoordinator` implementing `broker_conformance::Coordinator` (kind `Relay`, `blind`/`structural`, `lock_in` None, `self_host` **Backstop** — explicitly NOT the scarce-reachability exception, `delivery_path_gate` NoDeliveryPath, `metering` NotMetered/SignedReceiptsToPayer, `settlement` ExistingAssetsOnly, no token); `Descriptor::sign` wired to a real kotva-core `IdentityKey`. Crate docs carry the ROLES §4 conflation warning: Circuit Relay v2 relays two **libp2p peers**, distinct from `reachability-adapter`'s SNI-passthrough ingress for arbitrary non-libp2p clients. 12 tests (11 unit incl. a dedicated "not the scarce exception" conformance assertion + 1 real two-peer loopback Circuit-Relay-v2 test — a `ping` round trip through the relay between a peer with no other route, **not** `#[ignore]`d: proven reliable across repeated local runs, mirroring envoir's own non-ignored relay test). Workspace build+test+clippy clean. |
| W6 | **media-relay crate** (blind-routing) | DONE | new crate `crates/media-relay`: `MediaRelayCoordinator` implementing `broker_conformance::Coordinator` (kind `MediaRelay`, declared visibility **`blind-routing`/`structural`** — CONTRACT §5's own row, distinct from `relay`'s `blind` and `gateway`'s `terminating` — `is_verifiably_blind()` asserted true; `lock_in` None; `self_host` **Backstop**, explicitly NOT the scarce-reachability exception; `delivery_path_gate` NoDeliveryPath; `metering` NotMetered/SignedReceiptsToPayer toggle; `settlement` ExistingAssetsOnly, no token); `Descriptor::sign` wired to a real kotva-core `IdentityKey`. The real content is the **orchestration seam** (DIRECTION §3 bind-don't-reinvent, founder decision `[2026-07-23 sfu]`): a `MediaSfu` trait (`start`/`stop`/`status`) representing an SFU the coordinator schedules but never contains, plus two implementations — `ExternalSfu` (the reference adapter: generates a real coturn `turnserver.conf` fragment and a LiveKit-style JSON/YAML-compatible sidecar config, documents each sidecar's launch command, and manages the sidecar as a child process via `std::process`, degrading **honestly** to `SfuStatus::Unavailable` with a disclosed reason — never a fake `Running` — when the binary is absent from `PATH`, and re-checking child liveness on every `status()` call via `try_wait` so an exited process is reported `Stopped`, not stale-`Running`) and `MockSfu` (an in-process fake with no sidecar at all, for tests). Crate docs disclose the honest residual: `blind-routing` is not `blind` — the orchestrated SFU reads per-frame/packet metadata, RTP routing, sizes, timing, and the participant graph to forward at all (CONTRACT §3.1, SEC-9), and the relay structurally never holds a media-decryption key because SFrame (RFC 9605) keying rides MLS→SFrame end-to-end between the call's own participants (DIRECTION §7) — a client concern, never this crate's. No WebRTC/media/heavy dependency added; deps are `broker-economics`, `broker-conformance`, tag-pinned `kotva-core`, and `serde`/`serde_json` for config generation only. 23 tests (descriptor sign/verify + blind-routing-not-blind-not-terminating distinction + Backstop-not-scarce-exception + NoDeliveryPath + no-token + COORD-1 wrong-kind negative in `lib.rs`; `MockSfu` full start/stop/status lifecycle in `mock.rs`; `ExternalSfu` absent-binary-degrades-honestly for both coturn and LiveKit kinds, never-started-is-Stopped-not-Unavailable, a real short-lived process (`true`) observed transitioning to `Stopped` once it exits, coturn/LiveKit config-generation validity, and already-running rejection in `external.rs`). `cargo build --workspace` / `cargo test --workspace` / `cargo clippy --workspace --all-targets` all clean. Root `Cargo.toml` gained the one new member; no other crate edited. |
| W7 | **admin surface** | DONE | new crate `crates/admin`: kind-agnostic axum HTTP admin API composing broker-economics/broker-billing/broker-conformance (no other crate edited). Descriptor GET/PUT+sign (rejects a silent visibility downgrade per CONTRACT §3.2 unless `confirm_downgrade` discloses it, surfaces COORD-1..8 on every PUT); tariff GET/PUT+sign attached to the descriptor (rejects any `token` field — DIRECTION §5); metering + receipts GET view with the one-directional-audit caveat (CONTRACT §6, R-6) in every response, plus an operator-triggered `/billing/run/{payer}`; quota/rate policy GET/PUT (in-memory, declared not enforced); key mgmt (GET pubkey, POST rotate — re-signs the descriptor, keeps the old pubkey in an in-process history, never dropped, §2.1); `/conformance` runs `broker_conformance::check` over the live posture. Admin auth: constant-time bearer token, fail-closed default-deny (no token configured → every route 401, SEC-1); reference binary `wakala-admin` binds loopback by default. 29 tests (16 unit + 13 integration via `tower::ServiceExt::oneshot`). Workspace build+test+clippy clean. **Seams left deliberately:** in-memory descriptor/quota/meter/receipt stores (traits exist for `Meter`; descriptor/quota are not yet trait-abstracted); ephemeral key by default if no seed configured (loud warning); key rotation re-signs the descriptor only, not an already-attached tariff (still independently valid, just under the previous key); quota numbers are declared, not wired to an enforcement point. |
| W8 | **billing model** | DONE | new crate `crates/broker-billing`: kind-agnostic `Meter`/`ResourceKind` + `InMemoryMeter`; `TariffSchedule` (currency, per-`ResourceKind` price, free allowance) built via `broker_economics::Cbor::from_cv`, signed/verified through the real W3 `Tariff`; `ReceiptLog` issuing signed `UsageReceipt`s per billed line item, with the one-directional-audit residual (CONTRACT §6, R-6) documented in-code and demonstrated by a test (a fabricated, never-metered operation's receipt verifies identically to a real one's); `SettlementRail` trait (no-token invariant, DIRECTION §5) + the one reference adapter `InMemoryLedger` (explicitly a mock, double-entry, balances to zero) + x402-style `PaymentRequired`/`PaymentProof` data shapes; `StakeVerifier` seam + fail-closed `NoStakeRail` default (SEC-1, CONTRACT §6). `broker-economics`/`gateway`/`reachability-adapter`/`broker-conformance` untouched (kept self-contained per the wave's constraint). 27 tests, workspace clean (build+test+clippy) verified against the last-known-green HEAD (a concurrent W4b edit to `reachability-adapter` was in flight during verification; excluded from this wave's diff, not touched). |
| W9 | **remaining kind scaffolds** | DONE | six new thin crates, each a posture scaffold (no functionality — `broker_conformance::Coordinator` + a real signed `kotva-core`-backed descriptor only): `crates/indexer` (`Indexer` — corpus public plaintext, query channel `terminating`/`declared` default with a documented `attested`→`blind`/`attested` TEE option; `Gate::DerivedViewOnly` — ranks its own opt-in corpus, the §4 carve-out's own indexer example); `crates/labeler` (`Labeler` — `terminating`/`declared` over its label-input channel, documented as a channel-of-convenience since CONTRACT §5 marks visibility `n/a` for this kind; `Gate::DerivedViewOnly` — §4's *canonical* named carve-out example); `crates/matcher` (`Matcher` — `terminating`/`declared` default with a documented `attested`→`blind`/`attested` TEE option; `Gate::DerivedViewOnly` — pairs its own opt-in supply/demand pool); `crates/arbiter` (`Arbiter` — `terminating`/`declared` disclosed evidence; `Gate::NoDeliveryPath` — dispute resolution is neither a delivery path nor a derived view; stake documented as a `broker_billing::StakeVerifier` on-rail seam per CONTRACT §6, no dependency added, no stake field in the descriptor by construction); `crates/oracle` (`Oracle` — `terminating`/`declared` disclosed; `Gate::NoDeliveryPath`; same stake-seam note; docs cite DIRECTION §2's `ORACLE ⊂ ATTEST`); `crates/compute` (`Compute`, provisional per CONTRACT §5's own table — `terminating`/`declared` default with a documented `attested`→`blind`/`attested` TEE "blind compute" option, honestly noting the operator-trust-for-chip-vendor-trust tradeoff, R-4; `Gate::NoDeliveryPath`). All six: `#![forbid(unsafe_code)]`, `[lints] workspace = true`, `SelfHost::Backstop` (asserted-and-tested as NOT the scarce-reachability exception), `LockIn::None`, `Settlement::ExistingAssetsOnly` (no token), `Metering` toggle (`NotMetered`/`SignedReceiptsToPayer`) matching the `relay`/`reachability-adapter` pattern; depend only on `broker-economics` + `broker-conformance` (no direct `kotva-core` dep needed — `IdentityKey`/`Descriptor`/`Cbor` come through `broker_economics`'s re-export). 45 new unit tests total (indexer 8, labeler 7, matcher 8, arbiter 7, oracle 7, compute 8), each crate covering: signed-descriptor sign+verify, declared-visibility assertions, `check(&coordinator)` conformant (free + metered), the `DerivedViewOnly`-not-`Classification` distinction for indexer/labeler/matcher (with an in-test comment on what would flip it to a violation) or `NoDeliveryPath` for arbiter/oracle/compute, not-the-scarce-exception, no-token, and a COORD-1 wrong-kind-descriptor negative control. `cargo build --workspace` / `cargo test --workspace` / `cargo clippy --workspace --all-targets` all clean. Root `Cargo.toml` gained the six new members (only edit outside `crates/{indexer,labeler,matcher,arbiter,oracle,compute}/`). |
| W10 | **conformance harness expansion** | DONE | Runtime tests discharging `broker_conformance`'s two `Outcome::Behavioral` findings (COORD-1 signature, COORD-5 observed-vs-declared visibility) for the kinds with real implementations — `gateway` + `reachability-adapter`. `crates/gateway/tests/conformance_runtime.rs` (7 tests): COORD-1 real sign/verify + 2 tamper variants; COORD-5 `terminating`/`declared` disclosed-not-verified-blind + a real `InboundGateway::accept_message` plaintext round trip; COORD-6 two paired tests against the live `MxSession`/SPF wire path (same sender-authorization + different content ⇒ same outcome; same content + different authorization ⇒ different outcome), pinning the gate as authorization-based, not content-based. `crates/reachability-adapter/tests/conformance_runtime.rs` (8 tests): COORD-1 real sign/verify + 2 tamper variants; REACH-1a own-domain(structural)-vs-bare-vanity(declared) split, including "genuinely signed but still must-not-present-as-verified" for the vanity case; COORD-5's structural core — real `AdapterServer`/`auth`/`tunnel` wire path (reusing `ingress.rs`'s own fake-box pattern) splicing a real ClientHello + high-entropy ciphertext-shaped payload byte-identical end to end, a same-payload/different-SNI test proving routing keys on SNI alone, and a fail-closed-on-unregistered-name negative control; "holds no decrypting key" asserted both structurally (`AdapterServer`'s full field set + the crate's TLS-library-free dependency list, stated in-code) and by the byte-identical-ciphertext round trip. **Honest scope note (in-file):** no TLS crate dependency was available to drive a real end-to-end TLS handshake through the adapter without editing the crate manifest (out of scope for this wave's file-only constraint) — the claim tested is the narrower, correct one: the adapter itself never attempts to be the TLS party. **`relay` explicitly out of this wave's scope** (dispatched only for `crates/gateway/tests/` + `crates/reachability-adapter/tests/`, not `crates/relay/tests/`) even though `relay` itself already exists (W5, DONE) — its own COORD-1/COORD-5 runtime discharge (real libp2p Circuit-Relay-v2 sign/verify + an observed-vs-declared `blind`/`structural` check against the real two-peer relay test) is a natural, still-open follow-up, not a "not started yet" deferral. `cargo test -p gateway` (320, incl. the 7 new), `cargo test -p reachability-adapter` (32, incl. the 8 new), `cargo build --workspace`, `cargo clippy --workspace --all-targets` all clean; neither crate's lint posture (gateway's forbid-unsafe exemption, reachability-adapter's forbid-unsafe) was touched. Added test files only — no `src/`, no crate/root `Cargo.toml` edits. |
| W11 | **GitHub metadata + READMEs** | DONE | GH description+topics for wakala + envoir; wakala README (624cf9e) + envoir README (c7f8259, node-only, refreshed conformance numbers). Follow-ups (minor, non-breaking): envoir spec-link `dmtap`→`kotva`; `dmtap-sync-wasm/tests/native_trace.rs` sibling path `dmtap`→`kotva`; envoir node dispatch shim default binary name `envoir-gateway`→`wakala-gateway` (ENVOIR_GATEWAY_BIN override works) |
| W12 | **docs + CHANGELOG polish** | DONE | `CHANGELOG.md` gained a new top section for the Rust broker reference implementation (the carve, the broker model, all ten kinds with declared visibility, economics/admin/conformance, honest limits), old Go/JS relay-client history preserved unchanged below it. `crates/README.md` workspace-map table refreshed to all fourteen crates (media-relay marked "landing (W6)" — its crate directory exists on disk but was still uncommitted/not a workspace member when read, per the concurrent W6 agent; described only from this row, not from its code). Added one honest-limits paragraph to `broker-economics`'s crate doc (the only existing crate lacking one); all other existing crates already carried one. `cargo build --workspace` / `cargo test --workspace` verified green after the doc edit. |

## Loop mechanics
- Prefer **Workflow** (multi-agent) or parallel **Agent** (Sonnet) fan-out per wave.
- One writer per repo per iteration (avoid concurrent conflicting edits).
- If a wave needs a founder decision, log it to `COORDINATION.md`, mark the wave BLOCKED, move to the
  next unblocked wave — never guess on an irreversible product/business call.
